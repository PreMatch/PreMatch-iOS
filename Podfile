project 'PreMatch.xcodeproj'
workspace 'PreMatch.xcworkspace'

# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'PreMatch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PreMatch
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire', '~> 4.7'
  pod 'Sentry', :git => 'https://github.com/getsentry/sentry-cocoa.git', :tag => '4.3.1'
  pod 'Firebase/Analytics'

  target 'PreMatchTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'PreMatch Live' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PreMatch Live
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire', '~> 4.7'

end

target 'SevenPlusH' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SevenPlusH
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire', '~> 4.7'

  target 'SevenPlusHTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
